export enum Themes {
  Light = "Light",
  Dark = "Dark",
}

export enum Languages {
  Ru = "Ru",
  Eng = "Eng",
}

export enum Frames {
  Main = "Main",
  Dashboard = "Dashboard",
  Profiles = "Profiles",
}

export enum Url {
  Comments = "https://jsonplaceholder.typicode.com/comments",
  Designers = "https://jsonplaceholder.typicode.com/users",
}

export enum CommentNumbers {
  Visible = 4,
}

export enum IssueStatuses {
  New = "New",
  InProggess = "InProgress",
  Done = "Done",
}
